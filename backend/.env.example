# Example environment variables for backend/server.js
# Copy this file to .env and fill the values before running the backend

# Ticketmaster API key (required for suggest/search/details)
TICKETMASTER_API_KEY=YOUR_TICKETMASTER_API_KEY_HERE

# MongoDB Atlas connection string (optional for local dev but required to persist favorites)
# Example: mongodb+srv://<user>:<password>@cluster0.xyz.mongodb.net/?retryWrites=true&w=majority
MONGODB_URI=YOUR_MONGODB_URI_HERE

# Spotify API credentials (optional - used to fetch artist info)
SPOTIFY_CLIENT_ID=YOUR_SPOTIFY_CLIENT_ID
SPOTIFY_CLIENT_SECRET=YOUR_SPOTIFY_CLIENT_SECRET

# Port for the backend to listen on
PORT=8080

# NOTE: The frontend also needs environment variables (ipinfo token and Google Maps key).
# See frontend/README or frontend environment files for details.
